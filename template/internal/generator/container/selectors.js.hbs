import { createSelector } from '@reduxjs/toolkit';

import { initialState } from './slice';

const selectDomain = (state) => state.{{ camelCase ComponentName }} || initialState;

const select{{ properCase ComponentName }} = createSelector(
  [selectDomain],
  {{ camelCase ComponentName }}State => {{ camelCase ComponentName }}State,
);

export default select{{ properCase ComponentName }};